import { UnderAngeFilterDirective } from './under-ange-filter.directive';

describe('UnderAngeFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new UnderAngeFilterDirective();
    expect(directive).toBeTruthy();
  });
});
